/**
 * blacklist.js — DEPRECATED
 *
 * This file is superseded by blacklist-manage.js which provides the full
 * enhanced /blacklist command with regex support, test mode, and clear-all.
 *
 * This module intentionally exports nothing so the command loader skips it.
 */

// No export — loader skips files without a `name` property.
